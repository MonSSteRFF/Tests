{
  "name": "RimCalculator",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://monssterff.github.io/RimCalculator/",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.10",
    "prettier": "^2.8.1",
    "sass": "^1.57.1",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && npm run deployScript",
    "deployScript": "git add . && git commit -m 'deploy' && git subtree push --prefix build origin gh-pages"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
